services:
  whatsweb-backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
      - PORT=8080
      - ALLOWED_ORIGINS=http://localhost:3000
    restart: unless-stopped
